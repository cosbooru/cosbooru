#!/bin/sh

if docker version > /dev/null 2>&1; then
  exec docker compose -f docker-compose.yaml --env-file .env.production "${@:-up}"
else
  exec sudo docker compose -f docker-compose.yaml --env-file .env.production "${@:-up}"
fi
